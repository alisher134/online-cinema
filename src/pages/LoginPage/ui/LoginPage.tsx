import { LoginCreateForm } from '@/widgets/auth/login';

export const LoginPage = () => {
  return (
    <section>
      <h1>Hey there, welcome back</h1>
      <LoginCreateForm />
    </section>
  );
};
